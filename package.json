{
  "name": "today-mini-app",
  "private": true,
  "type": "module",
  "description": "today.calvin.sg — a Telegram Mini App showing the session in front of me and the rest of the day",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "test:auth": "node --test test/worker.http.test.mjs",
    "dev": "npx --yes wrangler@4.127.1 dev",
    "deploy": "npx --yes wrangler@4.127.1 deploy",
    "publish:week": "node scripts/publish.mjs"
  },
  "engines": { "node": ">=20" }
}
